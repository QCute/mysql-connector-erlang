{
    application, mysql_connector,
    [
        {description, "MySQL Connector for Erlang"},
        {vsn, "1.0.0"},
        {mod, {mysql_connector, []}},
        {modules, [mysql_connector]},
        {registered, []},
        {applications, [kernel, stdlib]},
        {env, [{mysql_connector, [{host, "localhost"}, {port, 3306}, {user, "root"}, {password, "root"}, {database, ""}, {encoding, "utf8mb4"}]}]}
    ]
}.
